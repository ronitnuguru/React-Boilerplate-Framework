import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div>React Boilerplate Framework. Build your apps on top of this framework</div>
    );
  }
}
